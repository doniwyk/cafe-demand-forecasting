DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cafe_forecasting

# When running via docker compose, DATABASE_URL is set in docker-compose.yml.
# Use the value below for local development (without Docker):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cafe_forecasting
